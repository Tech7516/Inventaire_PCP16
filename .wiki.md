# Project Summary

Application web collaborative de gestion d’inventaire destinée aux équipes opérationnelles. Les administrateurs configurent les lots, sous-lots et kits, tandis que les équipes réalisent sur mobile ou desktop des sessions historisées de vérification, d’inventaire et de désinfection.

Les sessions actives peuvent désormais être exécutées en parallèle sur plusieurs variantes d’un même lot. Seules les variantes déjà inventoriées sont verrouillées, tandis que les autres restent sélectionnables pour démarrer un nouvel inventaire.

# Project Module Description

- **Administration des lots et sous-lots** : configuration des lots utilisés pour générer les sessions.
- **Gestion des kits** : création et modification des kits personnalisables depuis la page d’administration.
- **Kits prédéfinis** : SNV, AERV, Mater/Accouchement et kit de risque biologique renforcé.
- **Kits personnalisables** : gestion manuelle des kits Membre arraché, Biologique et Pharmacie.
- **Menus déroulants des kits** : affichage des compositions et quantités dans l’inventaire.
- **Persistance des configurations** : sauvegarde cloud des définitions personnalisées et préférences d’instances.
- **Sessions collaboratives** : création, reprise, navigation et suivi des ensembles à contrôler.
- **Sessions multiples par lot** : plusieurs inventaires actifs peuvent être ouverts simultanément, notamment sur des variantes différentes.
- **Accès aux sessions** :
  - un bouton orange permet de rejoindre chaque session active ;
  - un bouton bleu permet de démarrer un nouvel inventaire sur une autre variante.
- **Verrouillage par variante** : seules les variantes déjà associées à une session active sont grisées et marquées « en cours » dans le menu déroulant.
- **Libellés des sessions** :
  - les sessions de vérification affichent « Rejoindre [variante] — DPS : [nom] » lorsque la variante est connue ;
  - les sessions de désinfection affichent « Rejoindre [variante] — Désinfection » lorsque la variante est connue ;
  - sans variante, les libellés restent « Rejoindre — DPS : [nom] » ou « Rejoindre — Désinfection ».
- **Sous-sections multi-instances** : gestion des cartes Lot B dans le Lot A et des cartes DSA dans le VPS.
- **Vérifications des sous-entités** : enregistrement des contrôles par sous-entité, instance et variante.
- **Validation des sessions** : sauvegarde désactivée tant que les instances requises ne sont pas complètes.
- **Désinfection des lots** : enregistrement des désinfections dans le journal.
- **Inventaires DSA, T7 et AMS** : génération, persistance et affichage des éléments dédiés.
- **Journal et rapports** : historisation des événements et calcul des divergences.
- **API et données** : FastAPI, SQLAlchemy asynchrone, PostgreSQL et Alembic.
- **Frontend** : interface React/TypeScript adaptée aux usages mobile et desktop.

# Directory Tree

```text
app/
├── backend/                      API FastAPI, modèles, services et migrations
│   ├── alembic/versions/         Historique des migrations Alembic
│   ├── data_models/              Schémas JSON des données métier
│   ├── models/                   Modèles SQLAlchemy persistés
│   ├── routers/                  Routes CRUD et consultation
│   └── services/                 Logique métier applicative
├── frontend/                     Application React/TypeScript
│   └── src/
│       ├── components/ui/
│       │   └── sonner.tsx        Notifications toast
│       ├── data/
│       │   └── lots.ts            Lots, variantes, inventaires et kits
│       ├── lib/
│       │   ├── configStore.ts     Persistance cloud des configurations
│       │   └── inventory-api.ts   Accès API, sessions et rapports
│       └── pages/
│           ├── Admin.tsx          Administration des lots et kits
│           ├── Index.tsx          Sélection, verrouillage et accès aux sessions
│           ├── Inventory.tsx      Inventaires directs et complétion des sessions
│           ├── Log.tsx            Journal des événements
│           └── SubEntities.tsx    Instances, variantes et contrôles
├── uploads/                      Fichiers téléversés
└── scripts/                      Scripts de démarrage et de packaging
```

# File Description Inventory

- `app/frontend/src/pages/Index.tsx` : charge et regroupe plusieurs sessions actives par lot, affiche un bouton de reprise pour chaque session, verrouille uniquement les variantes concernées et conserve un bouton de démarrage pour les autres variantes.
- `app/frontend/src/pages/Inventory.tsx` : gère les inventaires directs, la reprise, l’abandon et la complétion des sessions.
- `app/frontend/src/pages/SubEntities.tsx` : gère les sous-entités, variantes, instances Lot B et instances DSA VPS.
- `app/frontend/src/data/lots.ts` : définit les lots, variantes, inventaires, kits et articles.
- `app/frontend/src/lib/configStore.ts` : charge et sauvegarde les configurations de kits et préférences d’instances.
- `app/frontend/src/lib/inventory-api.ts` : gère les appels d’inventaire, sessions, complétions, abandons et rapports.
- `app/frontend/src/pages/Admin.tsx` : permet de créer, modifier et supprimer les articles des kits manuels.
- `app/frontend/src/pages/Log.tsx` : affiche et ordonne les événements de vérification et de désinfection.
- `app/frontend/src/components/ui/sonner.tsx` : configure les notifications.
- `app/backend/routers/` : expose les routes de gestion, session et consultation.
- `app/backend/data_models/` : contient les schémas des configurations, inventaires, sessions et rapports.
- `app/backend/models/` : définit les entités persistées en base.
- `app/backend/services/` : regroupe les traitements métier.
- `app/backend/alembic/versions/` : contient les migrations PostgreSQL.

# Technology Stack

- **Backend** : FastAPI, PostgreSQL, SQLAlchemy asynchrone, Alembic
- **Frontend** : React, TypeScript, Tailwind CSS
- **Composants UI** : shadcn/ui, Accordion
- **Notifications** : Sonner
- **Gestion des dépendances frontend** : pnpm
- **Validation** : lint et build frontend

# Usage

1. Installer les dépendances backend :

   ```bash
   pip install -r app/backend/requirements.txt
   ```

2. Installer les dépendances frontend :

   ```bash
   cd app/frontend
   pnpm install
   ```

3. Vérifier le lint frontend :

   ```bash
   pnpm run lint
   ```

4. Construire le frontend :

   ```bash
   pnpm run build
   ```

5. Démarrer les services selon la configuration de l’environnement de déploiement.

Le lint et le build frontend sont validés sans erreur.
