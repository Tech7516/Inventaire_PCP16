# Project Summary
This project is an advanced inventory management system designed to optimize operational efficiency and enhance user experience for businesses. It offers collaborative features that allow multiple users to perform inventory checks simultaneously. Key functionalities include user authentication, robust database management, file storage capabilities, and AI integration, providing comprehensive oversight of inventory processes.

# Project Module Description
The project consists of various functional modules, including:
- **Authentication:** User registration, login, and session management.
- **Database Management:** PostgreSQL integration for CRUD operations and table management.
- **File Storage:** Object storage for managing images, videos, and documents.
- **Edge Functions:** Custom server-side APIs built with FastAPI.
- **AI Capabilities:** Features for text, image, and audio generation, along with PDF analysis and voice transcription.
- **Collaborative Inventory Management:** Facilitates multiple users in managing inventory checks on the same lot, including session creation, validation, and abandonment.

# Directory Tree
```
app/
├── backend/
│   ├── core/                      # Core functionalities including auth and database
│   ├── dependencies/              # Dependency management for services
│   ├── models/                    # Data models for the application
│   ├── routers/                   # API route definitions
│   ├── schemas/                   # Schemas for data validation
│   ├── services/                  # Business logic and service layer
│   ├── lambda_handler.py          # AWS Lambda handler
│   └── main.py                    # Entry point for the backend application
├── frontend/
│   ├── src/                       # Frontend source code
│   ├── public/                    # Public assets
│   ├── components/                # UI components
│   ├── pages/                     # Page components for routing
│   ├── lib/                       # Library files, including API interactions
│   └── pnpm-lock.yaml             # Dependency lock file for pnpm
└── uploads/                       # Directory for uploaded files
```

# File Description Inventory
- **backend/**: Contains all backend-related files, including configuration, models, routers, and services.
- **frontend/**: Contains the frontend application code, including components, pages, assets, and dependency lock files.
- **uploads/**: Stores uploaded documents and files.
- **frontend/src/lib/inventory-api.ts**: Interacts with the inventory API using standard entity CRUD endpoints. Recently updated to omit null optional fields from payloads to prevent API errors.
- **frontend/src/pages/Inventory.tsx**: Updated to set the "quantité réelle" field to default to "0" when a consumable is unchecked (non-conforme), ensuring better data integrity and user experience.
- **frontend/src/pages/Report.tsx**: Updated to distinguish between shortages and surpluses with different colors and clearer wording. Shortages are displayed in amber with "Manque: N" and surpluses in blue with "Excédent: +N," improving readability.
- **frontend/src/pages/Log.tsx**: Updated to include a "Rapport" button in each log entry, allowing users to view the corresponding discrepancy report for the lot.

# Technology Stack
- **Backend:** FastAPI, PostgreSQL
- **Frontend:** React, TypeScript
- **Deployment:** Atoms Cloud
- **Styling:** Tailwind CSS

# Usage
1. **Install Dependencies:**
   - For backend: Run `pip install -r app/backend/requirements.txt`
   - For frontend: Run `pnpm install` in the `app/frontend/` directory.
   
2. **Build the Project:**
   - For the frontend: Run `pnpm run build` in the `app/frontend/` directory.

3. **Run the Application:**
   - Start the backend server using `uvicorn app.backend.main:app --reload`
   - Start the frontend development server using `pnpm start` in the `app/frontend/` directory.
